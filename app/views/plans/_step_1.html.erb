<%# locals: (step:, plan_type: nil, items: nil, item: nil, errors: nil) %>

<div class="p-8 text-center">
  <header class="mb-8">
    <h2 class="text-2xl font-black text-gray-800 tracking-tight">Select Plan Type</h2>
    <p class="text-gray-500 text-sm mt-1">What would you like to plan for today?</p>
  </header>

  <div class="grid grid-cols-2 gap-6">
    <%# Resonator Selection %>
    <%= link_to new_plan_path(plan_type: "Resonator", step: 2), 
        data: { turbo_frame: "modal" },
        class: "group flex flex-col items-center p-6 border-2 border-gray-100 rounded-2xl hover:border-blue-500 hover:bg-blue-50 transition-all duration-200 shadow-sm hover:shadow-md" do %>
      <div
        class="
          w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mb-4
          group-hover:scale-110 transition-transform
        "
      >
        <span class="text-2xl">👤</span>
      </div>
      <span class="font-bold text-gray-700 group-hover:text-blue-700">Resonator</span>
    <% end %>

    <%# Weapon Selection %>
    <%= link_to new_plan_path(plan_type: "Weapon", step: 2), 
        data: { turbo_frame: "modal" },
        class: "group flex flex-col items-center p-6 border-2 border-gray-100 rounded-2xl hover:border-orange-500 hover:bg-orange-50 transition-all duration-200 shadow-sm hover:shadow-md" do %>
      <div
        class="
          w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mb-4
          group-hover:scale-110 transition-transform
        "
      >
        <span class="text-2xl">⚔️</span>
      </div>
      <span class="font-bold text-gray-700 group-hover:text-orange-700">Weapon</span>
    <% end %>
  </div>
</div>
