<% if !user_signed_in? && plans.any? %>
  <div class="sync-banner">
    <div class="sync-banner-content">
      <div class="sync-banner-icon" aria-hidden="true">💾</div>
      <div>
        <h3 class="sync-banner-title">Sync your plans?</h3>
        <p class="sync-banner-text">
          You have
          <%= pluralize(plans.count, "plan") %>
          saved as a guest. Create an account to access them on any device.
        </p>
      </div>
    </div>

    <div class="sync-banner-actions">
      <%= link_to "Sync", new_user_registration_path, class: "sync-btn" %>
    </div>
  </div>
<% end %>
