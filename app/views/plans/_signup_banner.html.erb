<% if !user_signed_in? && plans.any? %>
  <div
    class="
      mb-8 p-6 bg-ctp-surface0 border-2 border-dashed border-ctp-mauve/40 rounded-2xl
      flex flex-col md:flex-row items-center justify-between gap-4
    "
  >
    <div class="flex items-center gap-4">
      <div class="text-3xl">💾</div>
      <div>
        <h3 class="text-ctp-mauve font-bold text-lg">Sync your plans?</h3>
        <p class="text-ctp-subtext0 text-sm">
          You have
          <%= pluralize(plans.count, "plan") %>
          saved as a guest. Create an account to access them on any device.
        </p>
      </div>
    </div>
    <div class="flex gap-3">
      <%= link_to "Sign Up",
      new_user_registration_path,
      class:
        "px-6 py-2 bg-ctp-mauve text-ctp-base rounded-xl font-bold hover:scale-105 transition-all" %>
      <%= link_to "Login",
      new_user_session_path,
      class:
        "px-6 py-2 border border-ctp-mauve text-ctp-mauve rounded-xl font-bold hover:bg-ctp-mauve/10 transition-all" %>
    </div>
  </div>
<% end %>
